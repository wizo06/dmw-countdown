# dmw-countdown

1. Install firebase service key inside `config/` as `serviceAccountKey.json`
2. `npm i`
3. `npm start`

## discord permissions

- view channels
- send messages
- add reactions
- manage messages
- read message history